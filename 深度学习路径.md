# HeteroMesh 深度学习路径（补充参考）

> 分析日期：2026-05-14 | 项目当前状态：第 5 课/里程碑 1（通信引擎完成）
> **⚠️ 主执行计划请见 `Plan/概览.md`（第 6 课 ~ 第 26 课）**
> 本文档作为补充参考，提供各知识点的学习资料和推荐阅读

---

## 一、项目深度诊断

### ✅ 已经做得好的

| 维度 | 评价 |
|------|------|
| 协议设计 | 自定义二进制协议 `0xCAFEBABE` 魔数 + 固定头 + varint，思路清晰 |
| 序列化对比 | JSON(Gson) vs 自研二进制(MessageCodec)，有 benchmark |
| 编解码器 | `LengthFieldBasedFrameDecoder` 处理粘包半包，实现正确 |
| 心跳机制 | `IdleStateHandler` + `HeartbeatHandler`，有单元测试和集成测试 |
| RPC 骨架 | `CompletableFuture` + requestId 匹配，支持并发乱序回复 |
| 测试覆盖 | 单元测试 + 集成测试 + 1000 消息压测，质量不错 |
| 学习笔记 | 每节课都写了学习总结和面试口述稿 |

### ⚠️ 深度不够的地方（核心问题）

| 问题 | 严重程度 | 说明 |
|------|----------|------|
| **Controller 几乎为空** | 🔴 | `HeteroMeshServer` 只是个 Netty Server 封装，没有节点管理、没有调度、没有路由、没有 Web 接口 |
| **Worker 几乎为空** | 🔴 | `WorkerClient` 只是在连接建立时发了条消息，没有任务执行、没有 GPU 推理、没有文件存储 |
| **一致性哈希未实现** | 🔴 | Readme 里写了，但代码中没有任何实现 |
| **Disruptor 未引入** | 🔴 | Plan 里写了第 2 周引入，pom.xml 里无依赖，代码里无使用 |
| **Dashboard 不存在** | 🟡 | Readme 写了 WebSocket + Chart.js，没有任何代码 |
| **Docker 未配置** | 🟡 | Readme 写了 Docker Compose，没有 docker-compose.yml |
| **Ollama/DJL 集成不存在** | 🟡 | 第 3 周计划中的 AI 推理，无任何代码 |
| **分布式文件存储不存在** | 🟡 | 第 4 周计划中的文件分片，无任何代码 |
| **protobuf 只写了 .proto 文件** | 🟡 | `message.proto` 已定义，但未集成到序列化流程中 |
| **无节点注册/发现机制** | 🟡 | ConnectionManager 用 channelId 做 key，没有 nodeId |
| **无任务状态机** | 🟡 | 没有任务状态定义和生命周期管理 |
| **无配置化** | 🟢 | 端口、地址都硬编码在代码里 |

**一句话结论：当前完成度约 30%，是一个质量不错的「通信引擎 demo」，离「分布式推理调度系统」还差约 70% 的核心功能。**

---

## 二、深入学习路线图

下面按照 6 个阶段、从浅入深，为每个阶段指定需要学习的知识 + 对应的代码实现 + 推荐的参考资料。

---

## 阶段 1：填补「节点管理」空白（1-2 天）

### 学习目标
让 Controller 能管理多个 Worker 节点的生命周期。

### 需要精学的内容

#### 1.1 Node Registry 设计模式
- 什么是**服务注册与发现**
- 主动注册 vs 被动心跳检测
- **参考：** Eureka 客户端注册原理、Consul 健康检查

#### 1.2 并发数据结构深入
- `ConcurrentHashMap` 的分段锁演进（Java 7 → Java 8）
- `ComputableFuture` 与 `ConcurrentHashMap.computeIfAbsent` 配合
- **参考：** 《Java 并发编程的艺术》第 6 章

#### 1.3 Worker 元数据设计
- 节点需要哪些信息：`nodeId`, `host`, `port`, `gpuType`, `vramFree`, `status`, `lastHeartbeat`, `load`
- 如何用 Protobuf 定义注册消息

### 代码实现

1. **定义 `NodeInfo` 类** — Worker 节点元数据结构
2. **实现 `NodeRegistry`** — 基于 `ConcurrentHashMap<String, NodeInfo>` 的注册表
3. **实现节点注册协议消息** — `REGISTER` / `REGISTER_ACK`（.proto 文件已有定义）
4. **改造 `ConnectionManager`** — 从 channelId 改为 nodeId 索引
5. **实现 `NodeManager` 类** — 管理节点上线、心跳超时剔除、节点列表查询

### 📘 必读资料

| 资料 | 链接/说明 |
|------|-----------|
| Netty 官方 User Guide | https://netty.io/wiki/user-guide.html |
| Eureka 注册中心原理 | Netflix Eureka wiki |
| Java ConcurrentHashMap 源码分析 | JDK 源码 + 网上分析文章 |

---

## 阶段 2：实现「一致性哈希调度」（2-3 天）

### 学习目标
实现带虚拟节点的、支持增删节点的一致性哈希环，并整合到 Controller 调度流程中。

### 需要精学的内容

#### 2.1 哈希算法进阶
- **普通取模哈希**的问题：节点增删导致大量 key 迁移
- **一致性哈希**原理：哈希环、顺时针查找
- **虚拟节点**：解决数据倾斜问题（150 个虚拟节点）
- **哈希函数选择**：`MD5` / `MurmurHash3` / `FNV-1a`，为什么不能用 `hashCode()`
- **参考：** 《大型网站技术架构》一致性哈希章节

#### 2.2 树形数据结构
- `TreeMap` 的 `tailMap` / `ceilingEntry` / `firstEntry` 实现环形查找
- 红黑树原理（了解即可）

#### 2.3 调度策略对比
- 一致性哈希 vs 最少连接数 vs 加权轮询
- 何时用哪种策略

### 代码实现

1. **实现 `ConsistentHashRouter`** — 泛型类，支持任意节点类型
   - `addNode(T node)`：加入节点并创建 virtualNodeCount 个虚拟节点
   - `removeNode(T node)`：移除节点及其虚拟节点
   - `route(String key)`：顺时针查找最近的节点
2. **集成到 Controller** — ServerHandler 收到 TASK_REQUEST 时调用 router 选择目标 Worker
3. **编写单元测试** — 验证节点分布均匀性、增删节点后最小化 key 迁移

### 📘 必读资料

| 资料 | 链接/说明 |
|------|-----------|
| 一致性哈希论文 | Karger et al., "Consistent Hashing and Random Trees" (1997) |
| MurmurHash3 | https://github.com/aappleby/smhasher |
| TreeMap 源码 | JDK `java.util.TreeMap` 源码 |
| Redis Cluster 哈希槽设计 | Redis 官方文档之 Cluster 规范 |

---

## 阶段 3：引入 Disruptor 构建高吞吐调度引擎（3-4 天）

### 学习目标
理解无锁编程、CPU 缓存行伪共享、Disruptor 环形缓冲区，并集成到 Controller 中。

### 需要精学的内容

#### 3.1 并发队列的演化
- `BlockingQueue`（锁竞争、上下文切换）
- `ConcurrentLinkedQueue`（CAS 无锁，但 O(1) 出队 + GC 压力）
- **Disruptor 的三个改进点：**
  - 环形数组（预分配，零 GC）
  - 缓存行填充（`@Contended` 注解，避免伪共享 False Sharing）
  - 无锁写入（序列号 + CAS + Memory Barrier）

#### 3.2 CPU 底层知识
- **伪共享 (False Sharing)**：两个变量在同一个缓存行，写一个变量导致另一个变量的缓存失效
- **缓存行 (Cache Line)**：64 字节对齐
- **Memory Barrier / volatile**：JMM 的 happens-before

#### 3.3 Disruptor 核心概念
- `RingBuffer`：环形缓冲区
- `Sequence` / `Sequencer`：序列号管理
- `EventHandler`：事件消费者
- `WaitStrategy`：等待策略（BlockingWaitStrategy / BusySpinWaitStrategy / YieldingWaitStrategy）

### 代码实现

1. **添加依赖** — `pom.xml` 引入 `com.lmax:disruptor:4.0.0`
2. **定义 `TaskEvent`** — Disruptor 的事件类（复用环形缓冲区槽位）
3. **定义 `TaskEventProducer`** — 发布新任务到 RingBuffer
4. **定义 `TaskEventHandler`** — 消费者：从 RingBuffer 取出任务，调用一致性哈希路由，通过 Netty 发送给目标 Worker
5. **改造 Controller** — 接收 HTTP/RPC 请求 → `TaskEventProducer.publish()` → Disruptor 异步处理

### 📘 必读资料

| 资料 | 链接/说明 |
|------|-----------|
| Disruptor 官方 GitHub | https://github.com/LMAX-Exchange/disruptor |
| Disruptor 原始论文 | "LMAX: How to Do 100K TPS at Less Than 1ms Latency" |
| 伪共享详解 | 《Java 并发编程实战》第 16 章 / CPU 缓存一致性协议 MESI |
| Martin Fowler 关于 LMAX 的文章 | https://martinfowler.com/articles/lmax.html |

---

## 阶段 4：Worker 真正「干活」— AI 推理集成（4-5 天）

### 学习目标
让 Worker 节点能真正利用 GPU 跑 AI 模型推理，包括模型加载、任务队列、状态管理和结果回传。

### 需要精学的内容

#### 4.1 AI 推理的 Java 方案对比
| 方案 | 说明 | 适用场景 |
|------|------|----------|
| Ollama HTTP API | 调用本地的 Ollama 服务 | 快速实现，支持多种模型 |
| DJL (Deep Java Library) | 纯 Java 深度学习库，支持 PyTorch/TensorFlow/ONNX | 细粒度控制 |
| JNI 直接调用 CUDA | 最底层，性能最好 | 不推荐，开发成本太高 |

#### 4.2 推荐路径：先用 Ollama 快速搭通，再用 DJL 做深度集成

##### Ollama 方式（1 天）
- 学习 Ollama REST API：`POST /api/generate`、`POST /api/chat`
- 用 Java HttpClient / OkHttp 调用

##### DJL 方式（3-4 天）
- DJL 引擎架构：`Predictor`、`Model`、`Translator`、`Criteria`
- ONNX Runtime 与 DJL 集成
- 模型预热（Warmup）：加载后先跑一次空推理，让 GPU 准备好
- 模型池化：`BlockingQueue<Model>` 管理多个模型实例

#### 4.3 任务状态机设计

```
RECEIVED → QUEUED → WARMING → INFERRING → COMPLETED
                                      ↘ FAILED
```

- 使用 `Enum` 定义任务状态
- 每个状态迁移触发对应的回调动作
- `CompletableFuture` 链式编排

#### 4.4 Java 21 虚拟线程
- 虚拟线程 vs 平台线程的区别
- 虚拟线程适合 I/O 密集型（HTTP 调用 Ollama、读写文件）
- `Executors.newVirtualThreadPerTaskExecutor()`
- 不适合 CPU 密集型（推理计算本身）

### 代码实现

1. **Worker 添加 Ollama HTTP 调用能力** — `OllamaService` 类
2. **实现 `TaskStateMachine`** — 任务状态定义 + 状态迁移
3. **实现 `TaskQueueManager`** — Worker 本地任务队列（Disruptor 或 BlockingQueue）
4. **实现模型预热** — Worker 启动时自动加载模型
5. **结果回传** — 推理完成后通过 Netty 的 RPC 传回 Controller
6. **压力测试** — 并发提交 100 个推理任务，观察吞吐

### 📘 必读资料

| 资料 | 链接/说明 |
|------|-----------|
| Ollama API 文档 | https://github.com/ollama/ollama/blob/main/docs/api.md |
| DJL 官方文档 | https://djl.ai/docs/ |
| DJL ONNX Runtime 引擎 | https://github.com/deepjavalibrary/djl/tree/master/engines/onnxruntime |
| Java 21 虚拟线程 | JEP 444 (Virtual Threads)，推荐阅读文档 |
| 状态机模式 | 《Design Patterns》State 模式章节 / UML 状态图 |

---

## 阶段 5：分布式文件存储（3-4 天）

### 学习目标
实现文件分片、Block 存储、元数据管理，能在大文件（如模型参数文件）上存/取。

### 需要精学的内容

#### 5.1 文件系统基础
- **零拷贝 (Zero-Copy)**：`FileChannel.transferTo()` / `MappedByteBuffer`
- 内核空间 vs 用户空间，DMA 直接内存访问
- `sendfile()` 系统调用
- mmap 与传统 read/write 性能对比

#### 5.2 分布式存储架构
- **HDFS 架构**：NameNode(元数据) + DataNode(数据) 类比于 Controller + Worker
- **文件切片 (Block/Chunk)**：大文件切分成固定大小（64MB/4MB）的 Block
- **副本机制**：每个 Block 存 2-3 份，提高可靠性
- **参考：** 《Hadoop 权威指南》第 3 章 HDFS 架构

#### 5.3 元数据存储
- **文件元数据**：`fileId`, `fileName`, `fileSize`, `blockSize`, `blockCount`, `createTime`
- **Block 元数据**：`blockId`, `fileId`, `blockIndex`, `nodeIds[]`(副本所在节点)
- 如何保证元数据一致性（乐观锁 `version` 字段）

### 代码实现

1. **定义文件存储协议消息** — `FILE_UPLOAD_REQUEST`, `FILE_UPLOAD_RESPONSE`, `FILE_DOWNLOAD_REQUEST`, `FILE_DOWNLOAD_RESPONSE`
2. **实现 `FileChunker`** — 文件切分工具
3. **实现 Controller 端 `FileMetadataManager`** — 元数据存储
4. **实现 Worker 端 `BlockStorageService`** — 零拷贝写入/读取本地文件
5. **上传流程**：客户端 → Controller(创建元数据) → 选择 Worker(一致性哈希) → 分片发送 → Worker 写盘
6. **下载流程**：客户端 → Controller(查元数据) → 定位 Worker → 读取分片 → 组装回复

### 📘 必读资料

| 资料 | 链接/说明 |
|------|-----------|
| FileChannel 零拷贝 | https://www.baeldung.com/java-filechannel-transferTo-transferFrom |
| MappedByteBuffer 深入 | 《深入理解 Java 虚拟机》第 2 章 Direct Memory |
| HDFS 架构论文 | "The Hadoop Distributed File System" (2010) |
| Netty 大文件传输 | https://netty.io/wiki/related-articles.html |
| Linux 零拷贝原理 | https://xie.infoq.cn/article/1b3f025a6b8f54e6b3c1c3f85 |

---

## 阶段 6：Dashboard 监控与系统加固（3-4 天）

### 学习目标
构建实时 Web Dashboard、系统 Docker 容器化部署、JMH 权威压测。

### 需要精学的内容

#### 6.1 WebSocket + Netty
- Netty 自带 `HttpServerCodec` + `WebSocketServerProtocolHandler`
- 实现 Controller 的 Web 端口（HTTP 任务提交 + WebSocket 实时推送）
- 心跳、QPS、节点状态推送

#### 6.2 前端基础
- Chart.js：折线图（QPS 曲线）、仪表盘（节点状态）
- HTML5 EventSource / WebSocket API

#### 6.3 Docker 容器化
- `Dockerfile` 多阶段构建（编译 + 运行）
- `docker-compose.yml` — Controller + 多个 Worker + Prometheus + Grafana（可选）
- 端口映射与持久化卷

#### 6.4 JMH 权威基准测试
- JMH 注解：`@Benchmark`、`@BenchmarkMode`、`@Warmup`、`@Measurement`
- `@State(Scope.Thread)` / `@State(Scope.Benchmark)`
- 避免 JIT 优化失效（`Blackhole` / `@CompilerControl`）

### 代码实现

1. **实现 `HttpServerBootstrap`** — Controller 新增 HTTP + WebSocket 端口
2. **实现 `TaskSubmitHandler`** — HTTP POST `/api/tasks` 提交任务
3. **实现 `MonitorWebSocketHandler`** — 实时推送节点状态、QPS、延迟
4. **编写 `dashboard.html`** — Chart.js 前端仪表盘
5. **编写 `Dockerfile`** 和 `docker-compose.yml`
6. **编写 JMH 基准测试** — 消息序列化、RPC 调用、完整链路压测

### 📘 必读资料

| 资料 | 链接/说明 |
|------|-----------|
| Netty 内置 HTTP+WebSocket 示例 | https://github.com/netty/netty/tree/4.1/example/src/main/java/io/netty/example/http/websocketx |
| Chart.js 官方文档 | https://www.chartjs.org/docs/latest/ |
| Docker Compose 文档 | https://docs.docker.com/compose/ |
| JMH 官方示例 | https://github.com/openjdk/jmh/tree/master/jmh-samples/src/main/java/org/openjdk/jmh/samples |

---

## 三、推荐学习顺序（30 天）

```
第 1-2 天   阶段 1：节点管理（NodeRegistry + Register 协议）
第 3-5 天   阶段 2：一致性哈希调度
第 6-9 天   阶段 3：Disruptor 高吞吐引擎
第 10-14 天 阶段 4：Ollama 推理集成（快速出成果）
第 15-18 天 阶段 4 进阶：DJL 深度集成 + 任务状态机
第 19-22 天 阶段 5：分布式文件存储（零拷贝 + Block 管理）
第 23-26 天 阶段 6：Dashboard + Docker + JMH
第 27-30 天 联调、压力测试、写 Readme、打包进简历
```

---

## 四、一个重要的建议：README 与实际代码的差距

当前 `Readme.md` 写得非常好（技术栈、架构图、进度表齐全），但有一个重要问题：

> Readme 里写的 `一致哈希(150 虚拟节点)`, `Disruptor`, `虚拟线程 + Disruptor`, `WebSocket + Chart.js` 等——**这些功能代码里一个都没有。**

这是一个 **"自夸型 Readme"**，面试官一旦追问细节就会露馅。

建议：
1. 先在 `Readme.md` 中如实标注功能状态
2. 按照本学习路径逐步实现后，再逐步展开描述

---

## 五、每个阶段需要读的书 & 文章

### 必读整本书

| 书名 | 重点章节 | 对应阶段 |
|------|----------|----------|
| 《Netty 实战》 | 第 4-9 章（ByteBuf、编解码器、EventLoop、引导） | 基础 |
| 《Java 并发编程的艺术》 | 第 2 章（volatile/CAS）、第 6 章（ConcurrentHashMap）、第 8 章（线程池） | 阶段 2-3 |
| 《大型网站技术架构》 | 第 7 章（分布式缓存与一致性哈希）、第 9 章（异步解耦） | 阶段 2-3 |
| 《深入理解 Java 虚拟机》 | 第 2 章（直接内存）、第 12 章（Java 内存模型） | 阶段 3-5 |

### 选读

| 书名 | 理由 |
|------|------|
| 《Designing Data-Intensive Applications》 | 第 6 章（分区/哈希路由），相当高屋建瓴 |
| 《Hadoop 权威指南》 | 第 3 章 HDFS 架构，是分布式存储的教科书式参考 |
| 《程序员修炼之道》 | 第 3 节（实用主义哲学），教你怎么写出可维护的代码 |

---

## 六、一个实用自检表

每做完一个阶段，问自己这几个问题：

- [ ] **代码能否解释清楚？** — 有没有看过自己写的代码，能否在面试时脱稿讲解？
- [ ] **异常路径想过吗？** — 网络断连？节点挂掉？消息乱序？文件写一半断电？
- [ ] **有测试吗？** — 至少有一个集成测试验证完整链路
- [ ] **性能心里有数吗？** — 跑过多少 QPS？延迟多少？瓶颈在哪？
- [ ] **跟竞品对比过吗？** — 你的系统跟 ruyuan-dfs / WXY-RPC / HiveCore 比，优势在哪、劣势在哪？

---

> **写于 2026-05-14 | 长门有希**
>
> *必要之事，不增不减。走完这个路线（Plan/概览.md 第 6 课 ~ 第 26 课），HeteroMesh 就能从一个通信引擎 demo 变成真正的分布式推理调度系统。*
>
> **注意：** 本文档的知识和资料推荐与 Plan 中的课程编号一一对应。具体编码实现顺序和每日节奏请以 Plan/概览.md 为准。本文件仅提供「学什么」和「去哪学」的深度参考。
